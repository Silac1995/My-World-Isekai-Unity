using UnityEngine;

// Force l'exécution même en mode édition si tu veux
[ExecuteInEditMode]
public class GrassInteraction : MonoBehaviour
{
//    void Update()
//    {
//        // Envoie la position du transform actuel (le centre de l'objet)
//        Shader.SetGlobalVector("_ObstaclePos", transform.position);

//        // Rayon forcé à 2 pour être sûr que l'herbe le sente passer
//        Shader.SetGlobalVector("_ObstacleRadius", new Vector4(2f, 0, 0, 0));

//        // Trace une ligne rouge verticale dans la fenêtre Scene
//        Debug.DrawRay(transform.position, Vector3.up * 5f, Color.red);
//    }
}